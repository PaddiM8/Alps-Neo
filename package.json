{
    "name": "sane",
    "version": "1.0.0",
    "description": "A simple and modern theme for Alps webmail",
    "scripts": {
        "build": "./build.sh",
        "dist": "./build.sh dist",
        "watch": "./build.sh && node ./watch.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "PaddiM8",
    "license": "MIT",
    "dependencies": {
        "esbuild": "0.17.14",
        "reconnecting-websocket": "^4.4.0",
        "trix": "^2.0.5"
    },
    "devDependencies": {
        "sass": "^1.60.0"
    }
}
